% \finalskip inserts a little vertical space to set off the hymn's final line
% (used instead of a special end-of-hymn symbol).  Place it in vertical mode,
% between the preceding \par and the final line.
\def\finalskip{\vskip 4pt}
